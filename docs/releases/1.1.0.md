- 模型服务交给 AI 助手配置：在设置里复制提示词，交给 Claude Code、Codex 等助手，它会配好并检查。
- 支持 OpenAI Chat Completions、Responses 与 Anthropic Messages 三种接口，不再局限于几家预设的服务商。
- 辞达有了命令行，可以查看和修改全部设置、检查模型服务是否可用。
- 辞达会自己检查并安装新版本，更新说明就写在面板里。
- 新的安装包是 DMG，把辞达拖进「应用程序」即可。
- 还没配置模型服务时，面板会告诉你从哪里开始；开机启动时不再弹出面板。
- 设置窗口在较小的屏幕上可以滚动，不再被程序坞挡住。
